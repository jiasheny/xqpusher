[default]
# 雪球Cookie参数（从网页获取）
xq_a_token = c074658bed381e88e09c92dc5b54841cb1133bfd
u = 1929296427

# (新增) 下面这两个token建议您也从Cookie中获取并填入，增强API稳定性
xq_id_token =
xq_r_token =

# 监控间隔（seconds=秒，minutes=分钟，hours=小时）
interval_type = seconds
interval_value = 30

# Server酱SendKey（从https://sct.ftqq.com获取）
# (可选) 您可以保留它作为全局通知，或留空禁用
#sct_send_key = SCT301974TTHlQUom8KWr07z6BeBZDTRYn

# (注意) 原有的 'cube_ids' 已被移除
# 脚本现在会自动从下面的 [notify_mapping] 中读取要监控的ID

[smtp]
# --- 邮件发送配置 (必填) ---
# 用于发送通知的SMTP服务器 (以QQ邮箱为例，其他邮箱请自行查询)
host = smtp.qq.com
# SMTP端口 (SSL加密通常为 465)
port = 465

# 您的“主邮箱”地址 (用这个邮箱来发送通知)
# (必填) 请修改为您自己的发件邮箱
user = 185542177@qq.com

# 您的“主邮箱”的SMTP授权码 (注意：不是邮箱密码！)
# (必填) 您需要登录邮箱网页版设置，开启SMTP服务并获取授权码
password = yzxvdulkfsrsbhab

[notify_mapping]
# --- 核心分发配置 (必填) ---
# 格式: 组合ID = 接收邮箱1, 接收邮箱2 (多个邮箱用逗号分隔)
# 脚本将只监控这里列出的组合ID

# (必填) 请修改下面的邮箱为您真实的“甲、乙、丙”的邮箱
# 示例：将 ZH1004909 的调仓通知发送给 a@example.com
ZH3540811 = 1339581757@qq.com

# 示例：将 ZH3540811 的调仓通知发送给 b@example.com
#ZH3540811 = b@example.com

# 示例：如果丙(c@example.com)也关注ZH3540811，可以这样写：
# ZH3540811 = b@example.com, c@example.com

# 您可以按此格式添加任意多个组合
# ZH_C = user_c@example.com
# ZH_D = user_d@example.com, user_e@example.com